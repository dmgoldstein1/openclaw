This instance of OpenClaw is being run in a Docker container.
Whenever you make changes, rebuild the Docker container if necessary, then use the Chrome MCP server to interact with the OpenClaw GUI to check your work. If at first you don't succeed, try, try again.
